{
  "name": "websocket",
  "scripts": {
    "start": "npm run server",
    "server": "live-server public --host=0.0.0.0 --port=3000"
  },
  "private": true,
  "devDependencies": {
    "live-server": "https://github.com/acco/live-server/tarball/master"
  },
  "dependencies": {
    "@otplib/preset-browser": "^12.0.1",
    "otplib": "12.0.1"
  }
}
